{{#results}}
	<div>
		{{title}}
		{{#characters.items}}
			{{name}}
		{{/characters.items}}
		{{#collection}}
			{{name}}
		{{/collection}}
		{{#creators.items}}
			{{name}}
		{{/creators.items}}
		{{#series.items}}
			{{name}}
		{{/series.items}}
		{{#stories.items}}
			{{name}}
		{{/stories.items}}

	</div>

{{/results}}
